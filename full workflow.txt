1. use SQL to get raw data and create train_test_split
2. perform EDA in order to inform mysself on what transformations the data requires 
    Typical EDA tasks:
	•	Checking missing values
	•	Inspecting distributions
	•	Detecting skew and outliers
	•	Understanding feature relationships
	•	Identifying data errors
	•	Checking correlations
	•	Finding class imbalance

Understand the data
(Goal: know what you’re working with)

☐ What is the prediction target?
☐ How many rows and columns?
☐ What are feature data types?
☐ What does each feature represent?
☐ Remove duplicate rows if present.

 → Inspect the  data
 (Goal: detect incomplete data)

☐ Which columns contain missing values?
☐ What percentage is missing?
☐ Is missingness meaningful or random?

→ Clean → Transform → Encode → Scale → Model

EDA → Decide transformations → Feature engineering → Modeling








eda and feature engineering

ultimately the goal of eda is to determine what features require which transformations to be applied to them in the preprocessing pipeline
to do so one must first understand the distrobution of the data in order to derive meaninnful insights 










When writing queries, ask:

What columns?  → SELECT
From where?    → FROM
Which rows?    → WHERE
How sorted?    → ORDER BY
How many rows? → LIMIT


COUNT()   -- number of rows
AVG()     -- average value
SUM()     -- total
MIN()     -- smallest value
MAX()     -- largest value